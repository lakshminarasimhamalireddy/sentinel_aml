# Sentinel AML React UI

Run the Spring Boot backend on port 8080, then:

```powershell
npm install
npm run dev
```

Open `http://127.0.0.1:5173`. Vite proxies `/api` requests to the backend, avoiding a development CORS dependency.
